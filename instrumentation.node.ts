/**
 * Node.js instrumentation entry point.
 * Server-side OpenTelemetry configuration can be added here later.
 */
export async function registerNodeInstrumentation(): Promise<void> {
  // No-op until server telemetry is configured.
}